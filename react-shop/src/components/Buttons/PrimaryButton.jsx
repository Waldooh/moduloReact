import React from 'react'

const PrimaryButton = ({text, type}) => {
    return (
        <div>
            <a href="https://kodemia.mx" className="btn btn-primary">Go somewhere</a>
        </div>
    );
};

export default PrimaryButton